/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { FilterPatientListComponent } from './filter-patient-list.component';

describe('Component: FilterPatientList', () => {
  it('should create an instance', () => {
    let component = new FilterPatientListComponent();
    expect(component).toBeTruthy();
  });
});
